Put Xunfei native .so files here by ABI folders.

Example structure:
- arm64-v8a/libmsc.so
- armeabi-v7a/libmsc.so

If the official SDK provides more native libraries, keep the original ABI folder structure.
